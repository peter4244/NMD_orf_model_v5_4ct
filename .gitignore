# Deposit ships CODE only. Everything under results_4ct/ (model inputs, the HDF5
# feature file, predictions, SHAP/attention/motif/GC exports) is regenerated by the
# pipeline — see README "Build order" (export_rds.R, relabel_tx_summary_4ct.R,
# data_prep.py, 03_train.py, the 0x_export_*.py scripts). Starting data lives in
# GEO (GSE329233). Nothing under results_4ct/ is version-controlled.
results_4ct/

figures/
__pycache__/
*.pyc
*.html
orf_model_report_v5_backup_*.Rmd
.DS_Store
